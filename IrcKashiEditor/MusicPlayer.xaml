<UserControl
    x:Class="IrcKashiEditor.MusicPlayer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IrcKashiEditor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:iconPacks="using:MahApps.Metro.IconPacks"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:controls="using:Windows.UI.Xaml.Controls" 
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="800"
    MinHeight="50">
    <Grid Background="#FF595959">
        <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
            <Button Margin="10 0 0 0" Template="{Binding Source={StaticResource PlayerButton}}"  Click="ChangePlayState">
                <iconPacks:PackIconMaterial Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center" Kind="{Binding IconMaterialKind}" />
            </Button>
            <Button Margin="0 0 0 0" Template="{Binding Source={StaticResource PlayerButton}}">
                <iconPacks:PackIconMaterial Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center" Kind="StopCircleOutline" />
            </Button>
            <muxc:DropDownButton>
                
            </muxc:DropDownButton>
        </StackPanel>
    </Grid>
</UserControl>
